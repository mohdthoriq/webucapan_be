model Effect {
  id           String @id @default(uuid()) @db.Uuid

  name         String @db.VarChar(100)

  code         String @unique @db.VarChar(100)

  theme_effects ThemeEffect[]

  created_at    DateTime @default(now())

  @@map("effects")
}